version: '3.8'
services:
  web:
    build:
      context: .
      dockerfile: Dockerfile.test
    command: pytest
    depends_on:
      - db
  db:
    image: postgres:13
    environment:
      - POSTGRES_USER=testuser
      - POSTGRES_PASSWORD=testpass
      - POSTGRES_DB=testdb
